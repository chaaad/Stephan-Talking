Stephan Talking - Translator API:



eg. the numbers translator:

	$.sTalk.translate.numbers(text); //returns modified text

	example:
	var out_text= $TT.numbers.process(in_text);

	where 'in_text' might be "123" and 'out_text' would be " won tU thrE "




New Translators can be added on, just follow the pattern of st_t-numbers.js
